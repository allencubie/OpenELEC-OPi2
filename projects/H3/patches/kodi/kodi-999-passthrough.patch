diff -Nur a/xbmc/cores/AudioEngine/Sinks/AESinkALSA.cpp pass/xbmc/cores/AudioEngine/Sinks/AESinkALSA.cpp
--- a/xbmc/cores/AudioEngine/Sinks/AESinkALSA.cpp	2016-04-15 18:52:10.000000000 +0200
+++ pass/xbmc/cores/AudioEngine/Sinks/AESinkALSA.cpp	2016-05-13 16:27:28.810240807 +0200
@@ -683,6 +683,15 @@
     fmt = SND_PCM_FORMAT_FLOAT;
     outconfig.format = AE_FMT_FLOAT;
   }
+  
+#if defined(HAS_CEDARX)
+  if (!AE_IS_RAW(inconfig.format))
+    snd_pcm_hw_params_set_raw(m_pcm, hw_params, 1); /* PCM */
+  else if (inconfig.format == AE_FMT_DTSHD)
+    snd_pcm_hw_params_set_raw(m_pcm, hw_params, 11); /* DTS_HD or DDP */
+  else
+    snd_pcm_hw_params_set_raw(m_pcm, hw_params, 2); /* AC3 but doesn't matter because driver combines everything which is not PCM, DTS_HD or DDP */  
+#endif
 
   /* try the data format */
   if (snd_pcm_hw_params_set_format(m_pcm, hw_params, fmt) < 0)
